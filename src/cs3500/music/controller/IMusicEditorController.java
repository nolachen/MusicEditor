package cs3500.music.controller;

/**
 * The controller interface for the music editor.
 */
public interface IMusicEditorController {
  /**
   * Starts the program.
   * I added more o's because I got a style point off because "go" was too short.
   * I kept it though because "go" was given to us and I didn't want to change the name.
   */
  void gooooo();
}
